// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Use this data source to retrieve information about an authentication strength policy within Azure Active Directory. This can be used to read either the built-in policies supplied by Microsoft, or custom policies created in the tenant.
 *
 * ## API Permissions
 *
 * The following API permissions are required in order to use this data source.
 *
 * When authenticated with a service principal, this data source requires one of the following application roles: `Policy.Read.AuthenticationMethod` or `Policy.Read.All`
 *
 * When authenticated with a user principal, this data source requires one of the following directory roles: `Conditional Access Administrator`, `Security Administrator`, `Security Reader` or `Global Administrator`
 *
 * ## Example Usage
 *
 * *Look up a built-in policy by display name*
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as azuread from "@pulumi/azuread";
 *
 * const example = azuread.getAuthenticationStrengthPolicy({
 *     displayName: "Multifactor authentication",
 * });
 * ```
 *
 * The display names of the built-in policies supplied by Microsoft are:
 *
 * * `Multifactor authentication`
 * * `Passwordless MFA`
 * * `Phishing-resistant MFA`
 *
 * *Look up a policy by object ID*
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as azuread from "@pulumi/azuread";
 *
 * const example = azuread.getAuthenticationStrengthPolicy({
 *     objectId: "00000000-0000-0000-0000-000000000004",
 * });
 * ```
 *
 * *Look up a policy managed elsewhere in the same configuration*
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as azuread from "@pulumi/azuread";
 *
 * const example = azuread.getAuthenticationStrengthPolicy({
 *     objectId: exampleAzureadAuthenticationStrengthPolicy.objectId,
 * });
 * ```
 */
export function getAuthenticationStrengthPolicy(args?: GetAuthenticationStrengthPolicyArgs, opts?: pulumi.InvokeOptions): Promise<GetAuthenticationStrengthPolicyResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("azuread:index/getAuthenticationStrengthPolicy:getAuthenticationStrengthPolicy", {
        "displayName": args.displayName,
        "objectId": args.objectId,
    }, opts);
}

/**
 * A collection of arguments for invoking getAuthenticationStrengthPolicy.
 */
export interface GetAuthenticationStrengthPolicyArgs {
    /**
     * The display name of the authentication strength policy.
     */
    displayName?: string;
    /**
     * The object ID of the authentication strength policy.
     *
     * > One of `displayName` or `objectId` must be specified.
     *
     * > **Tip** Display names are expected to be unique within a tenant, however this is not guaranteed by the API. Specify `objectId` where you need to be certain of matching a specific policy.
     */
    objectId?: string;
}

/**
 * A collection of values returned by getAuthenticationStrengthPolicy.
 */
export interface GetAuthenticationStrengthPolicyResult {
    /**
     * A list of allowed authentication methods combinations for this authentication strength policy.
     */
    readonly allowedCombinations: string[];
    /**
     * A list of `combinationConfigurations` blocks as documented below, which further constrain the allowed combinations for this policy.
     */
    readonly combinationConfigurations: outputs.GetAuthenticationStrengthPolicyCombinationConfiguration[];
    /**
     * The description of this authentication strength policy.
     */
    readonly description: string;
    /**
     * The display name of this authentication strength policy.
     */
    readonly displayName: string;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * The object ID of this combination configuration.
     */
    readonly objectId: string;
}
/**
 * Use this data source to retrieve information about an authentication strength policy within Azure Active Directory. This can be used to read either the built-in policies supplied by Microsoft, or custom policies created in the tenant.
 *
 * ## API Permissions
 *
 * The following API permissions are required in order to use this data source.
 *
 * When authenticated with a service principal, this data source requires one of the following application roles: `Policy.Read.AuthenticationMethod` or `Policy.Read.All`
 *
 * When authenticated with a user principal, this data source requires one of the following directory roles: `Conditional Access Administrator`, `Security Administrator`, `Security Reader` or `Global Administrator`
 *
 * ## Example Usage
 *
 * *Look up a built-in policy by display name*
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as azuread from "@pulumi/azuread";
 *
 * const example = azuread.getAuthenticationStrengthPolicy({
 *     displayName: "Multifactor authentication",
 * });
 * ```
 *
 * The display names of the built-in policies supplied by Microsoft are:
 *
 * * `Multifactor authentication`
 * * `Passwordless MFA`
 * * `Phishing-resistant MFA`
 *
 * *Look up a policy by object ID*
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as azuread from "@pulumi/azuread";
 *
 * const example = azuread.getAuthenticationStrengthPolicy({
 *     objectId: "00000000-0000-0000-0000-000000000004",
 * });
 * ```
 *
 * *Look up a policy managed elsewhere in the same configuration*
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as azuread from "@pulumi/azuread";
 *
 * const example = azuread.getAuthenticationStrengthPolicy({
 *     objectId: exampleAzureadAuthenticationStrengthPolicy.objectId,
 * });
 * ```
 */
export function getAuthenticationStrengthPolicyOutput(args?: GetAuthenticationStrengthPolicyOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetAuthenticationStrengthPolicyResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("azuread:index/getAuthenticationStrengthPolicy:getAuthenticationStrengthPolicy", {
        "displayName": args.displayName,
        "objectId": args.objectId,
    }, opts);
}

/**
 * A collection of arguments for invoking getAuthenticationStrengthPolicy.
 */
export interface GetAuthenticationStrengthPolicyOutputArgs {
    /**
     * The display name of the authentication strength policy.
     */
    displayName?: pulumi.Input<string | undefined>;
    /**
     * The object ID of the authentication strength policy.
     *
     * > One of `displayName` or `objectId` must be specified.
     *
     * > **Tip** Display names are expected to be unique within a tenant, however this is not guaranteed by the API. Specify `objectId` where you need to be certain of matching a specific policy.
     */
    objectId?: pulumi.Input<string | undefined>;
}
