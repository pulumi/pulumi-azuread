// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Manages an x509 certificate combination configuration for an authentication strength policy within Azure Active Directory.
 *
 * An x509 certificate combination configuration restricts which certificates, identified by their issuer subject key identifier (SKI) or certificate policy OID, satisfy the x509 certificate authentication method combinations. A policy may carry a separate configuration for each combination, but a given combination may be configured only once.
 *
 * ## API Permissions
 *
 * The following API permissions are required in order to use this resource.
 *
 * When authenticated with a service principal, this resource requires the following application roles: `Policy.ReadWrite.ConditionalAccess` and `Policy.Read.All`
 *
 * When authenticated with a user principal, this resource requires one of the following directory roles: `Conditional Access Administrator` or `Global Administrator`
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as azuread from "@pulumi/azuread";
 *
 * const example = new azuread.AuthenticationStrengthPolicy("example", {
 *     displayName: "Example Authentication Strength Policy",
 *     description: "Policy for demo purposes",
 *     allowedCombinations: [
 *         "x509CertificateSingleFactor",
 *         "x509CertificateMultiFactor",
 *     ],
 * });
 * const singleFactor = new azuread.AuthenticationStrengthPolicyX509CombinationConfiguration("single_factor", {
 *     authenticationStrengthPolicyId: example.objectId,
 *     appliesToCombinations: ["x509CertificateSingleFactor"],
 *     allowedIssuerSkis: ["9A4248C6AC8C2931AB2A86537818E92E7B6C97B6"],
 * });
 * const multiFactor = new azuread.AuthenticationStrengthPolicyX509CombinationConfiguration("multi_factor", {
 *     authenticationStrengthPolicyId: example.objectId,
 *     appliesToCombinations: ["x509CertificateMultiFactor"],
 *     allowedPolicyOids: ["1.2.3.4.5"],
 * });
 * ```
 *
 * ## Import
 *
 * x509 certificate combination configurations can be imported using the `id`, e.g.
 *
 * ```sh
 * $ pulumi import azuread:index/authenticationStrengthPolicyX509CombinationConfiguration:AuthenticationStrengthPolicyX509CombinationConfiguration example /policies/authenticationStrengthPolicies/00000000-0000-0000-0000-000000000000/combinationConfigurations/11111111-1111-1111-1111-111111111111
 * ```
 */
export class AuthenticationStrengthPolicyX509CombinationConfiguration extends pulumi.CustomResource {
    /**
     * Get an existing AuthenticationStrengthPolicyX509CombinationConfiguration resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AuthenticationStrengthPolicyX509CombinationConfigurationState, opts?: pulumi.CustomResourceOptions): AuthenticationStrengthPolicyX509CombinationConfiguration {
        return new AuthenticationStrengthPolicyX509CombinationConfiguration(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'azuread:index/authenticationStrengthPolicyX509CombinationConfiguration:AuthenticationStrengthPolicyX509CombinationConfiguration';

    /**
     * Returns true if the given object is an instance of AuthenticationStrengthPolicyX509CombinationConfiguration.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AuthenticationStrengthPolicyX509CombinationConfiguration {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AuthenticationStrengthPolicyX509CombinationConfiguration.__pulumiType;
    }

    /**
     * A set of allowed certificate issuer subject key identifier (SKI) values. At least one of `allowedIssuerSkis` or `allowedPolicyOids` must be specified.
     */
    declare public readonly allowedIssuerSkis: pulumi.Output<string[] | undefined>;
    /**
     * A set of allowed certificate policy OIDs. At least one of `allowedIssuerSkis` or `allowedPolicyOids` must be specified.
     */
    declare public readonly allowedPolicyOids: pulumi.Output<string[] | undefined>;
    /**
     * A set of x509 certificate authentication method combinations this configuration applies to. Possible values are `x509CertificateSingleFactor` and `x509CertificateMultiFactor`.
     */
    declare public readonly appliesToCombinations: pulumi.Output<string[]>;
    /**
     * The object ID of the authentication strength policy to which this combination configuration applies. Changing this field forces a new resource to be created.
     *
     * > Each combination specified in `appliesToCombinations` must also be specified in the `allowedCombinations` of the associated authentication strength policy, otherwise the API rejects the combination configuration.
     */
    declare public readonly authenticationStrengthPolicyId: pulumi.Output<string>;

    /**
     * Create a AuthenticationStrengthPolicyX509CombinationConfiguration resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AuthenticationStrengthPolicyX509CombinationConfigurationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AuthenticationStrengthPolicyX509CombinationConfigurationArgs | AuthenticationStrengthPolicyX509CombinationConfigurationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AuthenticationStrengthPolicyX509CombinationConfigurationState | undefined;
            resourceInputs["allowedIssuerSkis"] = state?.allowedIssuerSkis;
            resourceInputs["allowedPolicyOids"] = state?.allowedPolicyOids;
            resourceInputs["appliesToCombinations"] = state?.appliesToCombinations;
            resourceInputs["authenticationStrengthPolicyId"] = state?.authenticationStrengthPolicyId;
        } else {
            const args = argsOrState as AuthenticationStrengthPolicyX509CombinationConfigurationArgs | undefined;
            if (args?.appliesToCombinations === undefined && !opts.urn) {
                throw new Error("Missing required property 'appliesToCombinations'");
            }
            if (args?.authenticationStrengthPolicyId === undefined && !opts.urn) {
                throw new Error("Missing required property 'authenticationStrengthPolicyId'");
            }
            resourceInputs["allowedIssuerSkis"] = args?.allowedIssuerSkis;
            resourceInputs["allowedPolicyOids"] = args?.allowedPolicyOids;
            resourceInputs["appliesToCombinations"] = args?.appliesToCombinations;
            resourceInputs["authenticationStrengthPolicyId"] = args?.authenticationStrengthPolicyId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AuthenticationStrengthPolicyX509CombinationConfiguration.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AuthenticationStrengthPolicyX509CombinationConfiguration resources.
 */
export interface AuthenticationStrengthPolicyX509CombinationConfigurationState {
    /**
     * A set of allowed certificate issuer subject key identifier (SKI) values. At least one of `allowedIssuerSkis` or `allowedPolicyOids` must be specified.
     */
    allowedIssuerSkis?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * A set of allowed certificate policy OIDs. At least one of `allowedIssuerSkis` or `allowedPolicyOids` must be specified.
     */
    allowedPolicyOids?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * A set of x509 certificate authentication method combinations this configuration applies to. Possible values are `x509CertificateSingleFactor` and `x509CertificateMultiFactor`.
     */
    appliesToCombinations?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * The object ID of the authentication strength policy to which this combination configuration applies. Changing this field forces a new resource to be created.
     *
     * > Each combination specified in `appliesToCombinations` must also be specified in the `allowedCombinations` of the associated authentication strength policy, otherwise the API rejects the combination configuration.
     */
    authenticationStrengthPolicyId?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a AuthenticationStrengthPolicyX509CombinationConfiguration resource.
 */
export interface AuthenticationStrengthPolicyX509CombinationConfigurationArgs {
    /**
     * A set of allowed certificate issuer subject key identifier (SKI) values. At least one of `allowedIssuerSkis` or `allowedPolicyOids` must be specified.
     */
    allowedIssuerSkis?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * A set of allowed certificate policy OIDs. At least one of `allowedIssuerSkis` or `allowedPolicyOids` must be specified.
     */
    allowedPolicyOids?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * A set of x509 certificate authentication method combinations this configuration applies to. Possible values are `x509CertificateSingleFactor` and `x509CertificateMultiFactor`.
     */
    appliesToCombinations: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * The object ID of the authentication strength policy to which this combination configuration applies. Changing this field forces a new resource to be created.
     *
     * > Each combination specified in `appliesToCombinations` must also be specified in the `allowedCombinations` of the associated authentication strength policy, otherwise the API rejects the combination configuration.
     */
    authenticationStrengthPolicyId: pulumi.Input<string>;
}
