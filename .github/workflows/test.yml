# WARNING: This file is autogenerated - changes will be overwritten when regenerated by https://github.com/pulumi/ci-mgmt

name: "Test Provider"

on:
   workflow_call:
     inputs:
       version:
         required: true
         type: string
         description: Version of the provider to test

env:
  PR_COMMIT_SHA: ${{ github.event.client_payload.pull_request.head.sha }}
  MISE_ENV: test

  ARM_CLIENT_ID: d3b6ec3a-36fe-46c9-b3d9-5856a2e0e73c
  ARM_ENVIRONMENT: public
  ARM_SUBSCRIPTION_ID: 0282681f-7a9e-424b-80b2-96babd57a8a1
  ARM_TENANT_ID: 9605c22c-e585-4ea3-9b83-e90339719f8a
  PULUMI_API: https://api.pulumi-staging.io
  PULUMI_GO_DEP_ROOT: ${{ github.workspace }}/..
  PULUMI_LOCAL_NUGET: ${{ github.workspace }}/nuget
  RUN_OIDC_TESTS: ${{ github.event_name == 'pull_request' && 'true' || '' }}
  TF_APPEND_USER_AGENT: pulumi

jobs:
  test:
    # Integration tests are disabled on the v5 security-patch backport branch: the
    # examples acceptance tests require Azure AD credentials and are too old to
    # run reliably. This job is a no-op so that dependents declaring `needs: test`
    # (Sentinel, publish) stay green. Unit tests still run via prerequisites.yml
    # (`make test_provider`).
    runs-on: ubuntu-latest
    steps:
    - name: Integration tests disabled
      run: echo "Integration tests are disabled on the v5 security-patch backport branch."
